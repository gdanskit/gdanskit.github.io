
  setInterval(() => {
    const refreshBtns = document.querySelectorAll('button.quick-action-button.ng-star-inserted');
    refreshBtns[0].click();
    refreshBtns[2].click();
    refreshBtns[4].click();
    refreshBtns[6].click();
  }, 300000);
